import { defineSetupVue3 } from "@histoire/plugin-vue";
import "./assets/colors.css";
import "@fontsource-variable/figtree";

export const setupVue3 = defineSetupVue3(({ app }) => {
  // Register global plugins here if needed later
});
